<div class="container">
  <div class="row">
    <%= render 'new_book', book: @book, user: @user %>

    <div class="new-book col-md-8 offset-md-1"　>
      <h2>Books</h2>
      <table class="table">
      <thead>
         <tr>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
          </tr>
          <% @books.each do |book| %>
          <tr>
          <th></th>
          <th><%= book.book_title %></th>
          <th><%= book.opinion %></th>
         </tr>
          <% end %>

        </thead>
      </table>
      </div>
  </div>
</div>